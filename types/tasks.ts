export enum TaskType {
    LUNCH_BROWSER = "LUNCH_BROWSER",
}
export enum TaskParamType {
    STRING = "STRING",
    NUMBER = "NUMBER",
    BOOLEAN = "BOOLEAN",
    ARRAY = "ARRAY",
    OBJECT = "OBJECT",
}
